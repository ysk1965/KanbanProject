---
title: API Documentation
version: 1.6.0
updated: 2026-02-13
history:
  - v1.6.0: 2026-02-13
  - v1.5.0: 2026-02-08
  - v1.4.0: 2026-02-07
  - v1.3.0: 2026-02-07
  - v1.2.0: 2026-02-05
  - v1.1.0: 2026-02-02
  - v1.0.0: 2026-02-02
---

# API Documentation

## Base URL

```
http://localhost:8080/api/v1
```

## Authentication

- JWT Access Token (Bearer) in Authorization header
- Access Token 만료: 1시간 (3,600,000ms)
- Refresh Token 만료: 7일 (604,800,000ms)
- 알고리즘: HMAC SHA256

## 응답 형식

```json
// 성공
{
  "data": { ... }
}

// 에러
{
  "error": {
    "code": "FORBIDDEN",
    "message": "권한이 없습니다.",
    "details": { ... }
  }
}
```

## 권한 표기

| 표기 | 의미 |
|------|------|
| Public | 누구나 |
| User | 로그인 필요 |
| Board.Viewer+ | Viewer 이상 |
| Board.Member+ | Member 이상 |
| Board.Admin+ | Admin 이상 |
| Board.Owner | Owner만 |
| System Admin | 시스템 관리자 (systemRole: ADMIN) |

---

## Endpoints

### 1. Authentication (`/api/v1/auth`)

#### POST /auth/signup
사용자 회원가입

**Auth:** Public

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| email | string | O | 이메일 |
| password | string | O | 비밀번호 (8자 이상, 대/소문자, 숫자, 특수문자 포함) |
| name | string | O | 이름 |

**Response:** `TokenResponse`
```json
{
  "access_token": "eyJ...",
  "refresh_token": "eyJ...",
  "token_type": "Bearer",
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "name": "홍길동",
    "profile_image": null,
    "email_verified": false,
    "provider": "email",
    "system_role": "USER"
  }
}
```

#### POST /auth/login
이메일/비밀번호 로그인

**Auth:** Public

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| email | string | O | 이메일 |
| password | string | O | 비밀번호 |

**Response:** `TokenResponse`

#### POST /auth/google
Google OAuth 로그인

**Auth:** Public

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| token | string | O | Google ID Token |

**Response:** `TokenResponse`

#### POST /auth/refresh
Access Token 갱신

**Auth:** Public

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| refreshToken | string | O | Refresh Token |

**Response:** `TokenResponse`

#### POST /auth/logout
로그아웃 (Refresh Token 무효화)

**Auth:** User

**Response:**
```json
{ "message": "로그아웃 되었습니다." }
```

#### GET /auth/me
현재 사용자 정보

**Auth:** User

**Response:**
```json
{ "user_id": "uuid", "email": "user@example.com" }
```

#### GET /auth/verify-email?token={token}
이메일 인증

**Auth:** Public

#### POST /auth/resend-verification
인증 이메일 재전송

**Auth:** Public

**Request:** `{ "email": "user@example.com" }`

#### POST /auth/forgot-password
비밀번호 재설정 요청

**Auth:** Public

**Request:** `{ "email": "user@example.com" }`

#### POST /auth/reset-password
비밀번호 재설정

**Auth:** Public

**Request:** `{ "token": "reset-token", "newPassword": "newPass123!" }`

---

### 2. Users (`/api/v1/users`)

#### GET /users/me
현재 사용자 프로필 조회

**Auth:** User

**Response:** `User` 객체

#### PATCH /users/me
사용자 프로필 수정

**Auth:** User

**Request:** `UpdateProfileRequest`
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| name | string | X | 이름 |
| profileImage | string | X | 프로필 이미지 URL |
| theme | string | X | 테마 (dark, light) |

#### POST /users/me/password
비밀번호 변경

**Auth:** User

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| currentPassword | string | O | 현재 비밀번호 |
| newPassword | string | O | 새 비밀번호 |

#### DELETE /users/me
계정 삭제

**Auth:** User

---

### 3. Boards (`/api/v1/boards`)

#### POST /boards
보드 생성

**Auth:** User

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| name | string | O | 보드 이름 (max 100) |
| description | string | X | 설명 |

**Response:** `BoardResponse.Detail`

#### GET /boards
내 보드 목록 조회

**Auth:** User

**Response:** `List<BoardResponse.Simple>`

#### GET /boards/{boardId}
보드 상세 조회

**Auth:** Board.Viewer+

**Response:** `BoardResponse.Detail`

#### GET /boards/{boardId}/full
보드 전체 데이터 조회 (통합 API)

**Auth:** Board.Viewer+

> 보드 진입 시 필요한 모든 데이터를 한 번에 반환합니다. 기존 13개 개별 API 호출을 1개로 통합합니다.
> blocks, features, tasks, members, tags, milestones 등 포함

**Response:** `BoardResponse.Full`
```json
{
  "board": { ... },
  "blocks": [...],
  "features": [...],
  "tasks": [...],
  "members": [...],
  "tags": [...],
  "milestones": [...],
  "weight_levels": [...],
  "subscription": { ... },
  "limits": { ... }
}
```

#### PUT /boards/{boardId}
보드 정보 수정

**Auth:** Board.Admin+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| name | string | X | 보드 이름 |
| description | string | X | 설명 |
| workHoursPerDay | int | X | 일일 근무시간 |
| workStartTime | string | X | 근무 시작 시간 (HH:mm) |
| scheduleDisplayMode | string | X | 스케줄 표시 모드 |

#### DELETE /boards/{boardId}
보드 삭제

**Auth:** Board.Owner

#### PATCH /boards/{boardId}/star
보드 즐겨찾기 토글

**Auth:** Board.Viewer+

#### PATCH /boards/{boardId}/selected-milestone
선택 마일스톤 변경

**Auth:** Board.Admin+

**Request:** `{ "milestoneId": "uuid" }`

#### GET /boards/{boardId}/tier
보드 티어 정보 조회

**Auth:** Board.Viewer+

**Response:** `BoardResponse.TierInfo`

#### GET /boards/{boardId}/limits
보드 제한 사항 조회

**Auth:** Board.Viewer+

**Response:** `BoardResponse.Limits`

---

### 4. Blocks (`/api/v1/boards/{boardId}/blocks`)

#### GET /boards/{boardId}/blocks
블록 목록 조회

**Auth:** Board.Viewer+

**Response:** `BlockResponse.ListResponse`

#### POST /boards/{boardId}/blocks
커스텀 블록 생성

**Auth:** Board.Admin+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| name | string | O | 블록 이름 (max 50) |
| color | string | X | 색상 |

#### PUT /boards/{boardId}/blocks/{blockId}
커스텀 블록 수정

**Auth:** Board.Admin+

#### DELETE /boards/{boardId}/blocks/{blockId}
커스텀 블록 삭제

**Auth:** Board.Admin+

#### PUT /boards/{boardId}/blocks/reorder
블록 순서 변경

**Auth:** Board.Admin+

**Request:** `{ "blockIds": ["id1", "id2", ...] }`

---

### 5. Features (`/api/v1/boards/{boardId}/features`)

#### GET /boards/{boardId}/features
Feature 목록 조회

**Auth:** Board.Viewer+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| milestoneId | string | 마일스톤별 필터 (선택) |

**Response:** `FeatureResponse.ListResponse`

#### GET /boards/{boardId}/features/{featureId}
Feature 상세 조회

**Auth:** Board.Viewer+

#### POST /boards/{boardId}/features
Feature 생성

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| title | string | O | 제목 (max 200) |
| description | string | X | 설명 |
| color | string | X | 색상 |
| assigneeId | string | X | 담당자 ID |
| dueDate | string | X | 마감일 (YYYY-MM-DD) |

#### PUT /boards/{boardId}/features/{featureId}
Feature 수정

**Auth:** Board.Member+

#### DELETE /boards/{boardId}/features/{featureId}
Feature 삭제

**Auth:** Board.Member+

#### PUT /boards/{boardId}/features/reorder
Feature 순서 변경

**Auth:** Board.Member+

---

### 6. Tasks (`/api/v1/boards/{boardId}`)

#### GET /boards/{boardId}/tasks
Task 목록 조회

**Auth:** Board.Viewer+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| blockId | string | 블록별 필터 |
| featureId | string | Feature별 필터 |
| milestoneId | string | 마일스톤별 필터 |

#### GET /boards/{boardId}/tasks/{taskId}
Task 상세 조회

**Auth:** Board.Viewer+

#### POST /boards/{boardId}/features/{featureId}/tasks
Task 생성 (Feature 하위)

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| title | string | O | 제목 (max 200) |
| description | string | X | 설명 |
| startDate | string | X | 시작일 |
| dueDate | string | X | 마감일 |
| estimatedMinutes | int | X | 예상 소요시간(분) |

> Standard 보드: Task 10개 제한

#### PUT /boards/{boardId}/tasks/{taskId}
Task 수정

**Auth:** Board.Member+

#### DELETE /boards/{boardId}/tasks/{taskId}
Task 삭제

**Auth:** Board.Member+

#### PUT /boards/{boardId}/tasks/{taskId}/move
Task 블록 이동

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| blockId | string | O | 대상 블록 ID |
| position | int | X | 위치 |

> Done 블록 이동 시 자동 완료, Feature 진행률 업데이트

#### PUT /boards/{boardId}/tasks/{taskId}/dates
Task 날짜 수정

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| startDate | string | X | 시작일 |
| dueDate | string | X | 마감일 |

---

### 7. Tags (`/api/v1/boards/{boardId}/tags`)

#### GET /boards/{boardId}/tags
태그 목록 조회

**Auth:** Board.Viewer+

#### POST /boards/{boardId}/tags
태그 생성

**Auth:** Board.Admin+

**Request:** `{ "name": "태그명", "color": "#FF0000" }`

#### PUT /boards/{boardId}/tags/{tagId}
태그 수정

**Auth:** Board.Admin+

#### DELETE /boards/{boardId}/tags/{tagId}
태그 삭제

**Auth:** Board.Admin+

---

### 8. Checklists (`/api/v1/boards/{boardId}/tasks/{taskId}/checklists`)

#### GET /boards/{boardId}/tasks/{taskId}/checklists
체크리스트 조회

**Auth:** Board.Viewer+

#### POST /boards/{boardId}/tasks/{taskId}/checklists
체크리스트 항목 추가

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| title | string | O | 항목 제목 |
| assigneeId | string | X | 담당자 ID |
| startDate | string | X | 시작일 |
| dueDate | string | X | 마감일 |

#### PUT /boards/{boardId}/tasks/{taskId}/checklists/{itemId}
체크리스트 항목 수정

**Auth:** Board.Member+

#### DELETE /boards/{boardId}/tasks/{taskId}/checklists/{itemId}
체크리스트 항목 삭제

**Auth:** Board.Member+

#### PATCH /boards/{boardId}/tasks/{taskId}/checklists/{itemId}/toggle
체크리스트 완료 토글

**Auth:** Board.Member+

---

### 9. Comments (`/api/v1/boards/{boardId}/tasks/{taskId}/comments`)

#### GET /boards/{boardId}/tasks/{taskId}/comments
태스크 댓글 목록 조회

**Auth:** Board.Viewer+

**Response:**
```json
{
  "comments": [
    {
      "id": "uuid",
      "task_id": "uuid",
      "author": { "id": "uuid", "name": "홍길동", "profile_image": null },
      "content": "댓글 내용",
      "mentions": ["userId1", "userId2"],
      "created_at": "2026-02-02T12:00:00",
      "updated_at": "2026-02-02T12:00:00"
    }
  ],
  "total_count": 1
}
```

#### POST /boards/{boardId}/tasks/{taskId}/comments
댓글 작성

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| content | string | O | 댓글 내용 |
| mentions | string[] | X | 멘션할 사용자 ID 목록 |

**Response:** `CommentDetailResponse`

> 멘션된 사용자에게 Notification 자동 생성

#### PUT /boards/{boardId}/tasks/{taskId}/comments/{commentId}
댓글 수정 (본인만)

**Auth:** Board.Member+ (작성자 본인)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| content | string | O | 수정할 내용 |
| mentions | string[] | X | 멘션할 사용자 ID 목록 |

#### DELETE /boards/{boardId}/tasks/{taskId}/comments/{commentId}
댓글 삭제 (본인만)

**Auth:** Board.Member+ (작성자 본인)

#### DELETE /boards/{boardId}/tasks/{taskId}/comments/{commentId}/attachments/{attachmentId}
댓글 첨부파일 삭제 (본인만)

**Auth:** Board.Member+ (작성자 본인)

#### POST /boards/{boardId}/tasks/{taskId}/comments/{commentId}/reactions/toggle
댓글 리액션 토글

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| emoji | string | O | 이모지 (유니코드 또는 커스텀 이모지 이름) |

**Response:** `CommentResponse.ReactionsResponse`
```json
{
  "comment_id": "uuid",
  "reactions": [
    {
      "emoji": "👍",
      "count": 2,
      "users": ["userId1", "userId2"],
      "custom_emoji_url": null
    }
  ]
}
```

> 이미 리액션한 이모지를 다시 보내면 제거 (토글)

---

### 10. Members (`/api/v1/boards/{boardId}/members`)

#### GET /boards/{boardId}/members
멤버 목록 조회

**Auth:** Board.Viewer+

#### POST /boards/{boardId}/members/invite
이메일로 멤버 초대

**Auth:** Board.Admin+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| email | string | O | 초대할 이메일 |
| role | string | O | 역할 (ADMIN, MEMBER, VIEWER) |

#### PUT /boards/{boardId}/members/{memberId}/role
멤버 역할 변경

**Auth:** Board.Admin+

**Request:** `{ "role": "MEMBER" }`

#### DELETE /boards/{boardId}/members/{memberId}
멤버 제거

**Auth:** Board.Admin+

#### PUT /boards/{boardId}/members/{memberId}/color
멤버 담당자 색상 변경

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| assigneeColor | string | X | 색상 (indigo, purple, teal, rose, amber, emerald, null=초기화) |

**Response:** `MemberResponse.Detail`
```json
{
  "id": "uuid",
  "user": { "id": "uuid", "name": "홍길동", "email": "user@example.com", "profile_image": null },
  "role": "MEMBER",
  "joined_at": "2026-01-01T00:00:00",
  "invited_by": null,
  "assignee_color": "indigo",
  "display_order": 0
}
```

#### PUT /boards/{boardId}/members/reorder
멤버 표시 순서 변경

**Auth:** Board.Admin+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| memberIds | string[] | O | 멤버 ID 목록 (순서대로) |

**Response:** `MemberResponse.ListResponse`

---

### 11. Invite Links

#### GET /boards/{boardId}/invites
초대 링크 목록 조회

**Auth:** Board.Admin+

#### POST /boards/{boardId}/invites
초대 링크 생성

**Auth:** Board.Admin+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| role | string | O | 초대 역할 |
| maxUses | int | X | 최대 사용 횟수 |
| expiresAt | string | X | 만료일 |

#### DELETE /boards/{boardId}/invites/{inviteId}
초대 링크 비활성화

**Auth:** Board.Admin+

#### GET /invites/{code}
초대 링크 정보 조회

**Auth:** Public

#### POST /invites/{code}/accept
초대 수락

**Auth:** User

---

### 12. Milestones (`/api/v1/boards/{boardId}/milestones`)

#### GET /boards/{boardId}/milestones
마일스톤 목록 조회

**Auth:** Board.Viewer+

#### GET /boards/{boardId}/milestones/{milestoneId}
마일스톤 상세 조회

**Auth:** Board.Viewer+

#### POST /boards/{boardId}/milestones
마일스톤 생성

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| title | string | O | 제목 |
| description | string | X | 설명 |
| startDate | string | O | 시작일 |
| endDate | string | O | 종료일 |

#### PUT /boards/{boardId}/milestones/{milestoneId}
마일스톤 수정

**Auth:** Board.Member+

#### DELETE /boards/{boardId}/milestones/{milestoneId}
마일스톤 삭제

**Auth:** Board.Member+

#### POST /boards/{boardId}/milestones/{milestoneId}/features
마일스톤에 Feature 추가

**Auth:** Board.Member+

**Request:** `{ "featureIds": ["id1", "id2"] }`

#### DELETE /boards/{boardId}/milestones/{milestoneId}/features/{featureId}
마일스톤에서 Feature 제거

**Auth:** Board.Member+

#### GET /boards/{boardId}/milestones/{milestoneId}/allocations
마일스톤 할당 목록

**Auth:** Board.Viewer+

#### POST /boards/{boardId}/milestones/{milestoneId}/allocations
마일스톤 할당 생성

**Auth:** Board.Member+

#### PUT /boards/{boardId}/milestones/{milestoneId}/allocations/{allocationId}
마일스톤 할당 수정

**Auth:** Board.Member+

#### DELETE /boards/{boardId}/milestones/{milestoneId}/allocations/{allocationId}
마일스톤 할당 삭제

**Auth:** Board.Member+

---

### 13. Schedule (`/api/v1/boards/{boardId}/schedules`)

> Premium 전용

#### GET /boards/{boardId}/schedules
데일리 스케줄 조회

**Auth:** Board.Viewer+ (Premium)

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| date | string | 날짜 (YYYY-MM-DD, 필수) |
| assigneeIds | string[] | 담당자 ID 목록 (선택) |

**Response:** `ScheduleResponse.DailySchedule`

#### GET /boards/{boardId}/schedules/weekly
주간 스케줄 통합 조회

**Auth:** Board.Viewer+ (Premium)

> 기존 7개 개별 일일 스케줄 API 호출을 1개로 통합합니다.

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| startDate | string | 시작 날짜 (YYYY-MM-DD, 필수) |
| assigneeIds | string[] | 담당자 ID 목록 (선택) |

**Response:** `ScheduleResponse.WeeklySchedule`
```json
{
  "start_date": "2026-02-02",
  "end_date": "2026-02-08",
  "settings": { ... },
  "days": [
    {
      "date": "2026-02-02",
      "columns": [...]
    },
    {
      "date": "2026-02-03",
      "columns": [...]
    }
  ]
}
```

#### GET /boards/{boardId}/schedules/daily-full
데일리 풀뷰 조회 (스케줄 + 데일리 체크리스트 통합)

**Auth:** Board.Viewer+ (Premium)

> 스케줄 블록과 데일리 체크리스트를 한 번에 반환합니다.

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| date | string | 날짜 (YYYY-MM-DD, 필수) |
| assigneeIds | string[] | 담당자 ID 목록 (선택) |

**Response:** `ScheduleResponse.DailyFull`
```json
{
  "date": "2026-02-02",
  "settings": { ... },
  "columns": [...],
  "daily_checklists": [
    {
      "user": { "id": "uuid", "name": "홍길동", "profile_image": null },
      "items": [...]
    }
  ]
}
```

#### POST /boards/{boardId}/schedules
스케줄 블록 생성

**Auth:** Board.Member+ (Premium)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| assigneeId | string | O | 담당자 ID |
| scheduledDate | string | O | 날짜 (YYYY-MM-DD) |
| startTime | string | O | 시작 시간 (HH:mm) |
| endTime | string | O | 종료 시간 (HH:mm) |
| checklistItemId | string | X | 체크리스트 항목 ID |

**Response:** `ScheduleResponse.BlockDetail`

#### POST /boards/{boardId}/schedules/with-checklist-item
체크리스트 항목 생성과 함께 스케줄 블록 생성

**Auth:** Board.Member+ (Premium)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| assigneeId | string | O | 담당자 ID |
| scheduledDate | string | O | 날짜 (YYYY-MM-DD) |
| startTime | string | O | 시작 시간 (HH:mm) |
| endTime | string | O | 종료 시간 (HH:mm) |
| checklistItem | object | O | 체크리스트 항목 정보 |
| checklistItem.taskId | string | O | Task ID |
| checklistItem.title | string | O | 제목 (max 200) |
| checklistItem.startDate | string | X | 시작일 |
| checklistItem.dueDate | string | X | 마감일 |

**Response:** `ScheduleResponse.BlockDetail`

#### PUT /boards/{boardId}/schedules/{blockId}
스케줄 블록 수정

**Auth:** Board.Member+ (Premium)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| startTime | string | X | 시작 시간 (HH:mm) |
| endTime | string | X | 종료 시간 (HH:mm) |

**Response:** `ScheduleResponse.BlockDetail`

#### DELETE /boards/{boardId}/schedules/{blockId}
스케줄 블록 삭제

**Auth:** Board.Member+ (Premium)

#### GET /boards/{boardId}/schedules/settings
스케줄 설정 조회

**Auth:** Board.Viewer+ (Premium)

**Response:** `ScheduleResponse.SettingsInfo`

#### PUT /boards/{boardId}/schedules/settings
스케줄 설정 수정

**Auth:** Board.Admin+ (Premium)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| workHoursPerDay | int | X | 일일 근무시간 |
| workStartTime | string | X | 근무 시작 시간 (HH:mm) |
| scheduleDisplayMode | string | X | 스케줄 표시 모드 |

**Response:** `ScheduleResponse.SettingsInfo`

#### GET /boards/{boardId}/schedules/checklist-item/{checklistItemId}
특정 체크리스트 항목의 스케줄 블록 조회

**Auth:** Board.Viewer+ (Premium)

**Response:** `List<ScheduleResponse.BlockDetail>`

---

### 14. Activity Log (`/api/v1/boards/{boardId}/activities`)

#### GET /boards/{boardId}/activities
활동 로그 조회

**Auth:** Board.Viewer+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| cursor | string | 커서 (페이지네이션) |
| limit | int | 조회 수 (기본 20) |

#### GET /boards/{boardId}/activities/target/{targetType}/{targetId}
특정 대상의 활동 로그

**Auth:** Board.Viewer+

---

### 15. Subscription (`/api/v1/boards/{boardId}/subscription`)

#### GET /pricing
요금제 목록 조회

**Auth:** Public

#### GET /boards/{boardId}/subscription
구독 상태 조회

**Auth:** Board.Admin+

#### POST /boards/{boardId}/subscription/start
구독 시작 (Premium 전환)

**Auth:** Board.Owner

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| billingCycle | string | O | 결제 주기 (MONTHLY, YEARLY) |
| paymentMethodId | string | X | 결제 수단 ID |

#### PUT /boards/{boardId}/subscription/plan
구독 플랜 변경

**Auth:** Board.Owner

**Request:** `{ "billingCycle": "YEARLY" }`

#### DELETE /boards/{boardId}/subscription
구독 취소

**Auth:** Board.Owner

---

### 16. Statistics

#### GET /boards/{boardId}/statistics
보드 통계 조회

**Auth:** Board.Viewer+

#### GET /boards/{boardId}/statistics/personal
개인 통계 조회

**Auth:** Board.Viewer+

#### GET /boards/{boardId}/statistics/management
관리 통계 조회

**Auth:** Board.Admin+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| milestone_id | string | 마일스톤별 필터 (선택) |
| stagnant_task_days | int | 정체 Task 기준 일수 (기본: 3) |
| stuck_checklist_days | int | 정체 체크리스트 기준 일수 (기본: 2) |

#### GET /boards/{boardId}/statistics/weight-settings
가중치 설정 조회

**Auth:** Board.Viewer+

#### PUT /boards/{boardId}/statistics/weight-settings
가중치 설정 수정

**Auth:** Board.Admin+

---

### 17. Notifications (`/api/v1/notifications`)

#### GET /notifications
내 알림 목록 조회

**Auth:** User

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| cursor | datetime | 커서 (ISO 8601, 페이지네이션) |
| limit | int | 조회 수 (기본 20) |

**Response:**
```json
{
  "notifications": [
    {
      "id": "uuid",
      "type": "COMMENT_MENTION",
      "title": "새 멘션",
      "message": "홍길동님이 댓글에서 회원님을 멘션했습니다",
      "board_id": "uuid",
      "task_id": "uuid",
      "comment_id": "uuid",
      "sender": { "id": "uuid", "name": "홍길동", "profile_image": null },
      "read_at": null,
      "created_at": "2026-02-02T12:00:00"
    }
  ],
  "next_cursor": "2026-02-02T11:00:00",
  "has_more": true
}
```

#### GET /notifications/unread-count
읽지 않은 알림 수 조회

**Auth:** User

**Response:**
```json
{ "unread_count": 5 }
```

#### PUT /notifications/{notificationId}/read
알림 읽음 처리

**Auth:** User

**Response:** `NotificationResponse.Detail`

#### PUT /notifications/read-all
전체 알림 읽음 처리

**Auth:** User

**Response:**
```json
{ "message": "모든 알림을 읽음 처리했습니다" }
```

---

### 18. Board Checklists (`/api/v1/boards/{boardId}/checklist-items`)

#### GET /boards/{boardId}/checklist-items
보드 전체 체크리스트 항목 조회

**Auth:** Board.Viewer+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| assigneeId | string | 담당자별 필터 (선택) |
| isScheduled | boolean | 스케줄 할당 여부 필터 (선택) |

**Response:** `ChecklistResponse.BoardListResponse`

#### POST /boards/{boardId}/checklist-items/batch
여러 Task의 체크리스트 일괄 조회

**Auth:** Board.Viewer+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| taskIds | string[] | O | Task ID 목록 |

**Response:** `ChecklistBatchResponse`
```json
{
  "task_checklists": [
    {
      "task_id": "uuid",
      "items": [...],
      "total": 5,
      "completed": 3
    }
  ]
}
```

> N+1 문제 방지를 위한 배치 조회 API

---

### 19. Daily Checklists (`/api/v1/boards/{boardId}/daily-checklists`)

> Premium 전용

#### GET /boards/{boardId}/daily-checklists
데일리 체크리스트 조회

**Auth:** Board.Viewer+ (Premium)

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| date | string | 날짜 (YYYY-MM-DD, 필수) |

**Response:** `DailyChecklistResponse.ListResponse`
```json
{
  "date": "2026-02-02",
  "columns": [
    {
      "user": { "id": "uuid", "name": "홍길동", "profile_image": null },
      "items": [
        {
          "id": "uuid",
          "title": "체크리스트 항목",
          "position": 0,
          "task": { "id": "uuid", "title": "태스크명" },
          "feature": { "id": "uuid", "title": "피쳐명", "color": "#FF0000" }
        }
      ]
    }
  ]
}
```

#### POST /boards/{boardId}/daily-checklists
기존 체크리스트 항목을 데일리 체크리스트에 추가

**Auth:** Board.Member+ (Premium)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| checklistItemId | string | O | 체크리스트 항목 ID |
| assigneeId | string | O | 담당자 ID |
| assignedDate | string | O | 할당 날짜 (YYYY-MM-DD) |

**Response:** `DailyChecklistResponse.ItemResponse`

#### POST /boards/{boardId}/daily-checklists/with-item
새 체크리스트 항목 생성과 함께 데일리 체크리스트에 추가

**Auth:** Board.Member+ (Premium)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| taskId | string | O | Task ID |
| title | string | O | 제목 (max 200) |
| assigneeId | string | O | 담당자 ID |
| assignedDate | string | O | 할당 날짜 (YYYY-MM-DD) |

**Response:** `DailyChecklistResponse.ItemResponse`

#### PUT /boards/{boardId}/daily-checklists/{itemId}/position
데일리 체크리스트 항목 순서 변경

**Auth:** Board.Member+ (Premium)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| position | int | O | 변경할 위치 |

**Response:** `DailyChecklistResponse.ItemResponse`

#### DELETE /boards/{boardId}/daily-checklists/{itemId}
데일리 체크리스트 항목 삭제

**Auth:** Board.Member+ (Premium)

**Response:**
```json
{ "message": "데일리 체크리스트 항목이 삭제되었습니다" }
```

---

### 20. Weight Levels (`/api/v1/boards/{boardId}`)

#### GET /boards/{boardId}/weight-levels
보드 가중치 레벨 목록 조회

**Auth:** Board.Viewer+

**Response:** `WeightResponse.BoardWeightSettings`
```json
{
  "board_id": "uuid",
  "levels": [
    {
      "id": "uuid",
      "name": "Standard",
      "weight": 1.0,
      "color": "#6366f1",
      "position": 0,
      "is_default": true
    }
  ],
  "default_level_id": "uuid"
}
```

#### PUT /boards/{boardId}/weight-levels
보드 가중치 레벨 일괄 수정

**Auth:** Board.Admin+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| levels | array | O | 레벨 목록 |
| levels[].id | string | X | 기존 레벨 ID (신규 시 null) |
| levels[].name | string | O | 레벨 이름 |
| levels[].weight | number | O | 가중치 값 |
| levels[].color | string | X | 색상 |
| levels[].position | int | X | 위치 |
| default_level_id | string | X | 기본 레벨 ID |

**Response:** `WeightResponse.BoardWeightSettings`

#### POST /boards/{boardId}/tasks/{taskId}/weight
Task에 가중치 레벨 설정

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| weight_level_id | string | O | 가중치 레벨 ID |

**Response:** `WeightResponse.TaskWeightDetail`
```json
{
  "task_id": "uuid",
  "weight_level_id": "uuid",
  "weight_level_name": "High",
  "weight": 2.0,
  "color": "#ef4444"
}
```

#### GET /boards/{boardId}/tasks/{taskId}/weight
Task 가중치 레벨 조회

**Auth:** Board.Viewer+

**Response:** `WeightResponse.TaskWeightDetail`

> 가중치 미설정 시 기본값 반환 (Standard, weight: 1.0)

---

### 21. Admin (`/api/v1/admin`)

> System Admin 전용 (systemRole: ADMIN)
> 총 24개 엔드포인트

#### Users (8)

##### GET /admin/users
사용자 목록 조회

**Auth:** System Admin

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| page | int | 페이지 번호 (기본 0) |
| size | int | 페이지 크기 (기본 20) |
| search | string | 이름/이메일 검색 (선택) |

**Response:** `AdminResponse.UserList`
```json
{
  "users": [
    {
      "id": "uuid",
      "email": "user@example.com",
      "name": "홍길동",
      "profile_image": null,
      "email_verified": true,
      "auth_provider": "email",
      "system_role": "USER",
      "board_count": 3,
      "last_login_at": "2026-02-01T10:00:00",
      "created_at": "2026-01-01T00:00:00"
    }
  ],
  "total": 100,
  "page": 0,
  "size": 20
}
```

##### GET /admin/users/{userId}
사용자 상세 조회

**Auth:** System Admin

**Response:** `AdminResponse.UserDetail` (보드 목록 포함)

##### PATCH /admin/users/{userId}
사용자 시스템 역할 변경

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| systemRole | string | O | 시스템 역할 (USER, ADMIN) |

**Response:** `AdminResponse.UserSummary`

##### GET /admin/users/{userId}/boards
사용자의 보드 목록 조회

**Auth:** System Admin

**Response:** `AdminResponse.BoardList`

##### POST /admin/users/{userId}/deactivate
사용자 비활성화

**Auth:** System Admin

**Response:** `AdminResponse.UserSummary`

##### POST /admin/users/{userId}/activate
사용자 활성화

**Auth:** System Admin

**Response:** `AdminResponse.UserSummary`

##### POST /admin/users/{userId}/verify-email
이메일 강제 인증

**Auth:** System Admin

**Response:** `AdminResponse.UserSummary`

##### POST /admin/users/{userId}/send-password-reset
비밀번호 재설정 이메일 발송

**Auth:** System Admin

**Response:**
```json
{ "message": "비밀번호 재설정 이메일이 발송되었습니다" }
```

#### Boards (6)

##### GET /admin/boards
보드 목록 조회

**Auth:** System Admin

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| page | int | 페이지 번호 (기본 0) |
| size | int | 페이지 크기 (기본 20) |
| search | string | 보드명 검색 (선택) |
| tier | string | 티어 필터 (TRIAL, STANDARD, PREMIUM) |

**Response:** `AdminResponse.BoardList`

##### GET /admin/boards/{boardId}
보드 상세 조회

**Auth:** System Admin

**Response:** `AdminResponse.BoardDetail` (멤버 목록 포함)

##### DELETE /admin/boards/{boardId}
보드 삭제

**Auth:** System Admin

**Response:**
```json
{ "message": "보드가 삭제되었습니다" }
```

##### PATCH /admin/boards/{boardId}/tier
보드 티어 변경

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| tier | string | O | 티어 (STANDARD, PREMIUM) |

**Response:** `AdminResponse.BoardSummary`

##### POST /admin/boards/{boardId}/transfer-ownership
보드 소유권 이전

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| newOwnerId | string | O | 새 Owner 사용자 ID |

**Response:** `AdminResponse.BoardDetail`

##### PATCH /admin/boards/{boardId}/extend-trial
체험 기간 연장

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| days | int | O | 연장 일수 |

**Response:** `AdminResponse.BoardSummary`

#### Statistics (4)

##### GET /admin/statistics
시스템 통계 조회

**Auth:** System Admin

**Response:**
```json
{
  "total_users": 1000,
  "active_users": 500,
  "total_boards": 200,
  "trial_boards": 50,
  "standard_boards": 100,
  "premium_boards": 50,
  "active_subscriptions": 120
}
```

##### GET /admin/statistics/signups
가입 트렌드 조회

**Auth:** System Admin

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| period | string | 기간 (DAILY, WEEKLY, MONTHLY) |
| startDate | string | 시작일 (YYYY-MM-DD) |
| endDate | string | 종료일 (YYYY-MM-DD) |

**Response:** `AdminResponse.SignupTrends`

##### GET /admin/statistics/active-users
활성 사용자 트렌드 조회

**Auth:** System Admin

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| period | string | 기간 (DAILY, WEEKLY, MONTHLY) |
| startDate | string | 시작일 (YYYY-MM-DD) |
| endDate | string | 종료일 (YYYY-MM-DD) |

**Response:** `AdminResponse.ActiveUserTrends`

##### GET /admin/statistics/conversion
전환율 지표 조회

**Auth:** System Admin

**Response:** `AdminResponse.ConversionMetrics`

#### Subscriptions (1)

##### GET /admin/subscriptions
구독 목록 조회

**Auth:** System Admin

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| page | int | 페이지 번호 (기본 0) |
| size | int | 페이지 크기 (기본 20) |

**Response:** `AdminResponse.SubscriptionList`

#### Announcements (4)

##### GET /admin/announcements
공지사항 목록 조회

**Auth:** System Admin

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| page | int | 페이지 번호 (기본 0) |
| size | int | 페이지 크기 (기본 20) |

**Response:** `AdminResponse.AnnouncementList`

##### POST /admin/announcements
공지사항 생성

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| title | string | O | 제목 |
| content | string | O | 내용 |
| type | string | X | 유형 (INFO, WARNING, MAINTENANCE) |
| startAt | string | X | 노출 시작일 (ISO 8601) |
| endAt | string | X | 노출 종료일 (ISO 8601) |

**Response:** `AdminResponse.AnnouncementDetail`

##### PUT /admin/announcements/{announcementId}
공지사항 수정

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| title | string | X | 제목 |
| content | string | X | 내용 |
| type | string | X | 유형 |
| startAt | string | X | 노출 시작일 |
| endAt | string | X | 노출 종료일 |

**Response:** `AdminResponse.AnnouncementDetail`

##### DELETE /admin/announcements/{announcementId}
공지사항 삭제

**Auth:** System Admin

**Response:**
```json
{ "message": "공지사항이 삭제되었습니다" }
```

#### System/Maintenance (2)

##### GET /admin/system/maintenance
유지보수 상태 조회

**Auth:** System Admin

**Response:**
```json
{
  "maintenance": false,
  "message": null,
  "started_at": null,
  "estimated_end_at": null
}
```

##### POST /admin/system/maintenance
유지보수 모드 토글

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| enabled | boolean | O | 유지보수 모드 활성화 여부 |
| message | string | X | 유지보수 안내 메시지 |
| estimatedEndAt | string | X | 예상 종료 시간 (ISO 8601) |

**Response:**
```json
{
  "maintenance": true,
  "message": "서버 점검 중입니다",
  "started_at": "2026-02-07T10:00:00Z",
  "estimated_end_at": "2026-02-07T12:00:00Z"
}
```

#### Inquiries (4)

##### GET /admin/inquiries
문의 목록 조회

**Auth:** System Admin

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| page | int | 페이지 번호 (기본 0) |
| size | int | 페이지 크기 (기본 20) |
| status | string | 상태 필터 (OPEN, IN_PROGRESS, RESOLVED, CLOSED) |

**Response:** `AdminResponse.InquiryList`

##### GET /admin/inquiries/{inquiryId}
문의 상세 조회

**Auth:** System Admin

**Response:** `AdminResponse.InquiryDetail` (답변 목록 포함)

##### POST /admin/inquiries/{inquiryId}/reply
관리자 답변 작성

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| content | string | O | 답변 내용 |

**Response:** `AdminResponse.InquiryReplyDetail`

##### PATCH /admin/inquiries/{inquiryId}/status
문의 상태 변경

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| status | string | O | 상태 (OPEN, IN_PROGRESS, RESOLVED, CLOSED) |

**Response:** `AdminResponse.InquirySummary`

---

### 22. Test Data (`/api/v1/test`)

> 개발 환경 전용

#### POST /test/create-board
테스트 보드 생성

**Auth:** User

**Response:** `TestDataResponse`

> 개발/테스트용 샘플 데이터가 포함된 보드 자동 생성

---

### 23. Health & System

| Method | Endpoint | Description | Auth |
|--------|----------|-------------|------|
| GET | `/health` | 헬스체크 | Public |
| GET | `/actuator/**` | Actuator 엔드포인트 | Public |

---

### 24. File Upload (`/api/v1/files`)

#### POST /files/presign
Presigned URL 요청 (S3 모드 전용)

**Auth:** User

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| fileName | string | O | 파일명 |
| contentType | string | O | MIME 타입 |
| fileSize | number | O | 파일 크기 (bytes) |

**Response (S3 mode):**
```json
{
  "mode": "presigned",
  "temp_key": "string",
  "upload_url": "string"
}
```

**Response (Local mode):**
```json
{
  "mode": "direct",
  "message": "Presigned URL not supported. Use POST /api/v1/files/upload instead."
}
```

#### POST /files/upload
파일 직접 업로드

**Auth:** User

**Request:** `multipart/form-data`
- `file`: MultipartFile (필수)

**Response:**
```json
{
  "temp_key": "string",
  "preview_url": "string"
}
```

> 임시 저장소에 저장. temp_key를 이후 작업(댓글 첨부 등)에서 사용

---

### 25. Board Comments (`/api/v1/boards/{boardId}/comments`)

#### GET /boards/{boardId}/comments/summary
보드 댓글 요약 조회 (주간 요약용)

**Auth:** Board.Viewer+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| authorId | string | 작성자 ID (필수) |
| startDate | string | 시작일 YYYY-MM-DD (필수) |
| endDate | string | 종료일 YYYY-MM-DD (필수) |

**Response:**
```json
{
  "comments": [
    {
      "id": "uuid",
      "task_id": "uuid",
      "task_title": "태스크명",
      "content": "댓글 내용",
      "created_at": "2026-02-05T12:00:00"
    }
  ],
  "total_count": 5
}
```

---

### 26. Slack Webhook (`/api/v1/boards/{boardId}/slack-webhook/me`)

> 보드별 개인 Slack Webhook 설정 관리

#### GET /boards/{boardId}/slack-webhook/me
내 Slack Webhook 설정 조회

**Auth:** Board.Member+

**Response:** `SlackWebhookResponse.Detail`
```json
{
  "id": "uuid",
  "board_id": "uuid",
  "user_id": "uuid",
  "webhook_url": "https://hooks.slack.com/services/T.../B.../xxx",
  "channel_name": "#project-alerts",
  "enabled": true,
  "created_at": "2026-02-07T10:00:00Z",
  "updated_at": "2026-02-07T10:00:00Z"
}
```

#### PUT /boards/{boardId}/slack-webhook/me
Slack Webhook 생성/수정 (Upsert)

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| webhookUrl | string | O | Slack Webhook URL (`^https://hooks.slack.com/services/...`) |
| channelName | string | X | 채널 이름 (표시용) |

**Response:** `SlackWebhookResponse.Detail`

> Webhook URL은 `https://hooks.slack.com/services/` 로 시작해야 합니다.

#### DELETE /boards/{boardId}/slack-webhook/me
Slack Webhook 삭제

**Auth:** Board.Member+

**Response:**
```json
{ "message": "Slack webhook이 삭제되었습니다" }
```

#### POST /boards/{boardId}/slack-webhook/me/test
Slack Webhook 테스트 발송

**Auth:** Board.Member+

**Response:** `SlackWebhookResponse.TestResult`
```json
{
  "success": true,
  "message": "테스트 메시지가 성공적으로 전송되었습니다"
}
```

---

### 27. Inquiry (`/api/v1/inquiries`)

> 사용자 문의 (1:1 문의)

#### POST /inquiries
문의 등록

**Auth:** User

**Request:** `multipart/form-data`
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| title | string | O | 제목 |
| content | string | O | 내용 |
| files[] | MultipartFile[] | X | 첨부파일 (최대 5개) |

**Response:** `InquiryResponse.Detail`
```json
{
  "id": "uuid",
  "title": "문의 제목",
  "content": "문의 내용",
  "status": "OPEN",
  "files": [
    {
      "id": "uuid",
      "file_name": "screenshot.png",
      "file_url": "https://...",
      "file_size": 102400
    }
  ],
  "replies": [],
  "created_at": "2026-02-07T10:00:00Z",
  "updated_at": "2026-02-07T10:00:00Z"
}
```

#### GET /inquiries
내 문의 목록 조회

**Auth:** User

**Response:** `InquiryResponse.ListResponse`
```json
{
  "inquiries": [
    {
      "id": "uuid",
      "title": "문의 제목",
      "status": "OPEN",
      "has_unread_reply": false,
      "created_at": "2026-02-07T10:00:00Z",
      "updated_at": "2026-02-07T10:00:00Z"
    }
  ],
  "total": 5
}
```

#### GET /inquiries/{inquiryId}
문의 상세 조회

**Auth:** User (본인 문의만)

**Response:** `InquiryResponse.Detail` (답변 포함)

#### POST /inquiries/{inquiryId}/replies
사용자 답변 작성

**Auth:** User (본인 문의만)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| content | string | O | 답변 내용 |

**Response:** `InquiryResponse.ReplyDetail`
```json
{
  "id": "uuid",
  "content": "추가 설명입니다",
  "author_type": "USER",
  "author": { "id": "uuid", "name": "홍길동" },
  "created_at": "2026-02-07T11:00:00Z"
}
```

#### GET /inquiries/unread-count
읽지 않은 답변 수 조회

**Auth:** User

**Response:**
```json
{ "unread_count": 2 }
```

---

### 28. Notification Preferences (`/api/v1/boards/{boardId}/notification-preferences/me`)

> 보드별 개인 알림 설정 관리 (인앱 + Slack, 타입별 토글)

#### GET /boards/{boardId}/notification-preferences/me
내 알림 설정 조회

**Auth:** Board.Member+

**Response:** `NotificationPreferenceResponse.Detail`
```json
{
  "id": "uuid",
  "board_id": "uuid",
  "comment_mention_enabled": true,
  "checklist_assigned_enabled": true,
  "task_comment_enabled": true,
  "slack_comment_mention_enabled": true,
  "slack_checklist_assigned_enabled": true,
  "slack_task_comment_enabled": true,
  "created_at": "2026-02-07T10:00:00Z",
  "updated_at": "2026-02-07T10:00:00Z"
}
```

> 설정이 없는 경우 모든 항목 `true` (기본값) 반환

#### PUT /boards/{boardId}/notification-preferences/me
알림 설정 생성/수정 (Upsert)

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| commentMentionEnabled | boolean | X | 댓글 멘션 인앱 알림 |
| checklistAssignedEnabled | boolean | X | 체크리스트 배정 인앱 알림 |
| taskCommentEnabled | boolean | X | 태스크 댓글 인앱 알림 |
| slackCommentMentionEnabled | boolean | X | 댓글 멘션 Slack 알림 |
| slackChecklistAssignedEnabled | boolean | X | 체크리스트 배정 Slack 알림 |
| slackTaskCommentEnabled | boolean | X | 태스크 댓글 Slack 알림 |

**Response:** `NotificationPreferenceResponse.Detail`

> null이 아닌 필드만 업데이트됩니다.

---

### 29. System (`/api/v1/system`)

> 시스템 상태 및 공지사항 조회

#### GET /system/status
시스템 상태 조회

**Auth:** Public

**Response:**
```json
{
  "maintenance": false,
  "message": null
}
```

> 유지보수 모드 활성화 시 `maintenance: true` 와 안내 메시지 반환

#### GET /system/announcements/active
활성 공지사항 조회

**Auth:** User

**Response:**
```json
{
  "announcements": [
    {
      "id": "uuid",
      "title": "서비스 업데이트 안내",
      "content": "새로운 기능이 추가되었습니다",
      "type": "INFO",
      "start_at": "2026-02-07T00:00:00Z",
      "end_at": "2026-02-14T00:00:00Z",
      "created_at": "2026-02-06T10:00:00Z"
    }
  ]
}
```

---

### 30. Reports (`/api/v1/boards/{boardId}/reports`)

> AI 리포트 생성 및 조회

#### POST /boards/{boardId}/reports
AI 리포트 생성

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| reportType | string | O | 리포트 타입 (TEAM, PERSONAL) |
| periodStart | string | O | 기간 시작일 (YYYY-MM-DD) |
| periodEnd | string | O | 기간 종료일 (YYYY-MM-DD) |
| language | string | X | 언어 (ko, en) 기본: ko |
| targetUserId | string | X | 대상 사용자 ID (PERSONAL 타입 시) |

**Response:** `ReportResponse.Detail`
```json
{
  "id": "uuid",
  "board_id": "uuid",
  "report_type": "PERSONAL",
  "target_user_id": "uuid",
  "target_user_name": "홍길동",
  "period_start": "2026-02-01",
  "period_end": "2026-02-07",
  "content": "## 주간 리포트\n...",
  "generated_by": { "id": "uuid", "name": "홍길동" },
  "created_at": "2026-02-08T10:00:00Z",
  "updated_at": "2026-02-08T10:00:00Z"
}
```

> TEAM 리포트: Admin+ 권한 필요. PERSONAL 리포트: 본인 또는 Admin+ 생성 가능
> 일일 1회 제한 (per user per board)

#### GET /boards/{boardId}/reports
리포트 목록 조회

**Auth:** Board.Viewer+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| report_type | string | TEAM 또는 PERSONAL (선택) |
| target_user_id | string | 대상 사용자 ID (선택) |

**Response:** `ReportResponse.ListResponse`
```json
{
  "reports": [...],
  "can_generate_today": true
}
```

#### GET /boards/{boardId}/reports/{reportId}
리포트 상세 조회

**Auth:** Board.Viewer+

**Response:** `ReportResponse.Detail`

#### POST /boards/{boardId}/reports/{reportId}/regenerate
리포트 재생성

**Auth:** Board.Member+

**Response:** `ReportResponse.Detail`

> 데이터 변경이 감지된 경우에만 재생성 (SHA-256 해시 비교)

---

### 31. Daily Standup Config (`/api/v1/boards/{boardId}/standup-config`)

> 데일리 스탠드업 자동 발송 설정

#### GET /boards/{boardId}/standup-config
스탠드업 설정 조회

**Auth:** Board.Viewer+ (Premium)

**Response:** `StandupConfigResponse.Detail`
```json
{
  "id": "uuid",
  "board_id": "uuid",
  "enabled": true,
  "send_hour": 9,
  "send_minute": 0,
  "send_hour_utc": 0,
  "send_minute_utc": 0,
  "timezone": "Asia/Seoul",
  "language": "ko",
  "last_sent_at": "2026-02-08T00:00:00Z",
  "created_at": "2026-02-08T10:00:00Z",
  "updated_at": "2026-02-08T10:00:00Z"
}
```

> 설정이 없는 경우 204 No Content

#### PUT /boards/{boardId}/standup-config
스탠드업 설정 생성/수정 (Upsert)

**Auth:** Board.Admin+ (Premium)

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| enabled | boolean | X | 활성화 여부 |
| sendHour | int | X | 발송 시간 (0-23, 로컬 타임존) |
| sendMinute | int | X | 발송 분 (0-59) |
| timezone | string | X | 타임존 (예: Asia/Seoul) |
| language | string | X | 언어 (ko, en) |

**Response:** `StandupConfigResponse.Detail`

> 로컬 시간 -> UTC 자동 변환 저장

---

### 32. Meetings (`/api/v1/boards/{boardId}/meetings`)

> 보드별 미팅 관리 (CRUD, 반복 회의, AI 전사/요약, 노트 저장)
> 총 11개 엔드포인트

#### GET /boards/{boardId}/meetings
날짜별 미팅 목록 조회

**Auth:** Board.Viewer+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| date | string | 날짜 (YYYY-MM-DD, 필수) |

**Response:** `List<MeetingResponse.Summary>`
```json
[
  {
    "id": "uuid",
    "title": "스프린트 회의",
    "meeting_date": "2026-02-13",
    "start_time": "10:00:00",
    "end_time": "11:00:00",
    "color": "#8B5CF6",
    "participant_count": 5,
    "recurrence_rule": "WEEKLY",
    "recurrence_group_id": "uuid"
  }
]
```

#### GET /boards/{boardId}/meetings/range
날짜 범위별 미팅 목록 조회 (캘린더 뷰)

**Auth:** Board.Viewer+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| startDate | string | 시작 날짜 (YYYY-MM-DD, 필수) |
| endDate | string | 종료 날짜 (YYYY-MM-DD, 필수) |

**Response:** `List<MeetingResponse.Summary>`

> 캘린더 뷰에서 월간/주간 범위의 미팅을 한 번에 조회합니다.

#### GET /boards/{boardId}/meetings/{meetingId}
미팅 상세 조회

**Auth:** Board.Viewer+

**Response:** `MeetingResponse.Detail`
```json
{
  "id": "uuid",
  "title": "스프린트 회의",
  "meeting_date": "2026-02-13",
  "start_time": "10:00:00",
  "end_time": "11:00:00",
  "memo": "회의 메모 내용",
  "transcript": "전사 텍스트...",
  "color": "#8B5CF6",
  "recurrence_rule": "WEEKLY",
  "recurrence_group_id": "uuid",
  "recurrence_end_date": "2026-06-30",
  "created_by": {
    "id": "uuid",
    "name": "홍길동",
    "profile_image": null
  },
  "participants": [
    { "id": "uuid", "name": "김철수", "profile_image": null }
  ],
  "ai_suggestions": null,
  "created_at": "2026-02-10T10:00:00",
  "updated_at": "2026-02-13T09:00:00"
}
```

#### POST /boards/{boardId}/meetings
미팅 생성

**Auth:** Board.Member+

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| title | string | O | 제목 (max 200) |
| meetingDate | string | O | 미팅 날짜 (YYYY-MM-DD) |
| startTime | string | X | 시작 시간 (HH:mm) |
| endTime | string | X | 종료 시간 (HH:mm) |
| memo | string | X | 메모 |
| color | string | X | 색상 (기본: #8B5CF6) |
| recurrenceRule | string | X | 반복 규칙 ("WEEKLY", "BIWEEKLY") |
| recurrenceEndDate | string | X | 반복 종료 날짜 (YYYY-MM-DD) |

**Response:** `MeetingResponse.Detail` (201 Created)

> `recurrenceRule` 지정 시 `recurrenceEndDate`까지 반복 미팅이 자동 생성됩니다.
> 동일한 `recurrence_group_id`로 그룹화됩니다.

#### PUT /boards/{boardId}/meetings/{meetingId}
미팅 수정

**Auth:** Board.Member+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| scope | string | 수정 범위 (기본: THIS_ONLY). THIS_ONLY: 해당 미팅만, THIS_AND_FUTURE: 해당 미팅 및 이후 반복 미팅 |

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| title | string | X | 제목 (max 200) |
| meetingDate | string | X | 미팅 날짜 (YYYY-MM-DD) |
| startTime | string | X | 시작 시간 (HH:mm) |
| endTime | string | X | 종료 시간 (HH:mm) |
| memo | string | X | 메모 |
| color | string | X | 색상 |

**Response:** `MeetingResponse.Detail`

> 반복 미팅에서 `scope=THIS_AND_FUTURE` 사용 시, 해당 미팅을 포함한 이후 모든 반복 미팅이 함께 수정됩니다.

#### DELETE /boards/{boardId}/meetings/{meetingId}
미팅 삭제

**Auth:** Board.Member+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| scope | string | 삭제 범위 (기본: THIS_ONLY). THIS_ONLY: 해당 미팅만, THIS_AND_FUTURE: 해당 미팅 및 이후 반복 미팅 |

**Response:**
```json
{ "message": "회의가 삭제되었습니다" }
```

> 반복 미팅에서 `scope=THIS_AND_FUTURE` 사용 시, 해당 미팅 날짜 이후의 모든 반복 미팅이 함께 삭제됩니다.

#### POST /boards/{boardId}/meetings/{meetingId}/notify
참가자에게 회의록 알림 발송

**Auth:** Board.Member+

**Response:**
```json
{ "message": "회의록 알림을 보냈습니다" }
```

#### POST /boards/{boardId}/meetings/{meetingId}/transcribe
음성 파일 전사 (Speech-to-Text)

**Auth:** Board.Member+

**Request:** `multipart/form-data`
- `file`: MultipartFile (음성 파일, 필수)

**Response:** `MeetingResponse.TranscriptResult`
```json
{
  "meeting_id": "uuid",
  "transcript": "전사된 텍스트 내용..."
}
```

> 최대 파일 크기: 100MB. 대용량 파일은 FFmpeg를 사용해 청크 분할 후 전사됩니다.

#### PUT /boards/{boardId}/meetings/{meetingId}/transcript
전사 텍스트 수동 수정

**Auth:** Board.Member+

**Request:**
```json
{ "transcript": "수정된 전사 텍스트" }
```

**Response:** `MeetingResponse.TranscriptResult`

#### POST /boards/{boardId}/meetings/{meetingId}/ai-organize
AI 회의록 정리 및 제안 생성

**Auth:** Board.Member+

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| language | string | 언어 (선택, 기본: 자동 감지) |

**Response:** `MeetingAIResponse.Suggestions`

> 전사 텍스트를 기반으로 AI가 회의 요약, 액션 아이템, Feature/Task 제안을 생성합니다.

#### POST /boards/{boardId}/meetings/{meetingId}/ai-apply
AI 제안 적용 (Feature/Task 생성)

**Auth:** Board.Member+

**Request:** `MeetingAIRequest.Apply`

**Response:** `MeetingAIResponse.ApplyResult`

> AI가 제안한 Feature/Task를 실제 보드에 생성합니다.

#### POST /boards/{boardId}/meetings/{meetingId}/save-to-note
미팅 내용을 노트로 저장

**Auth:** Board.Member+

**Response:** `NoteResponse.Detail` (201 Created)

> 미팅 메모, 전사 내용, AI 요약 등을 노트 문서로 자동 변환하여 저장합니다.

---

### 33. Monitoring (`/api/v1/admin/monitoring`)

> System Admin 전용 - 시스템 모니터링 대시보드
> 총 6개 엔드포인트

#### GET /admin/monitoring/dashboard
통합 모니터링 대시보드 조회

**Auth:** System Admin

**Response:** `MonitoringResponse.Dashboard`
```json
{
  "jvm": {
    "heap_used": 134217728,
    "heap_max": 536870912,
    "heap_usage_percent": 25.0,
    "non_heap_used": 67108864,
    "live_threads": 30,
    "peak_threads": 45,
    "gc_pause_count": 12,
    "gc_pause_total_ms": 150.5
  },
  "hikari": {
    "active_connections": 3,
    "idle_connections": 7,
    "pending_connections": 0,
    "total_connections": 10,
    "max_connections": 10,
    "usage_percent": 30.0
  },
  "api": {
    "total_requests": 15000,
    "total_errors": 23,
    "error_rate": 0.15,
    "avg_response_ms": 45.2,
    "top_slowest_endpoints": [
      {
        "endpoint": "/api/v1/boards/{boardId}/reports",
        "http_method": "POST",
        "request_count": 50,
        "avg_response_ms": 2500.0,
        "max_response_ms": 8000.0,
        "p95_response_ms": 5000.0,
        "error_count": 1
      }
    ]
  },
  "cloud_watch": {
    "ec2": {
      "cpu_utilization": 15.3,
      "network_in": 1048576.0,
      "network_out": 524288.0
    },
    "rds": {
      "cpu_utilization": 8.5,
      "database_connections": 12,
      "freeable_memory_mb": 512.0,
      "read_iops": 100.0,
      "write_iops": 50.0
    }
  },
  "server_time": "2026-02-13T10:00:00Z"
}
```

> JVM 메모리/쓰레드, HikariCP 커넥션 풀, API 요청/에러율, CloudWatch EC2/RDS 메트릭을 통합 반환합니다.

#### GET /admin/monitoring/api-metrics/history
API 메트릭 히스토리 조회 (차트용)

**Auth:** System Admin

**Query Params:**
| Param | Type | Description |
|-------|------|-------------|
| hours | int | 조회 기간 (시간 단위, 기본: 24) |

**Response:** `MonitoringResponse.ApiMetricHistory`
```json
{
  "snapshots": [
    {
      "endpoint": "/api/v1/boards/{boardId}/full",
      "http_method": "GET",
      "snapshot_time": "2026-02-13T09:00:00",
      "request_count": 120,
      "avg_response_ms": 85.5,
      "max_response_ms": 450.0,
      "p95_response_ms": 200.0,
      "p99_response_ms": 350.0,
      "error_count": 0,
      "error_rate": 0.0
    }
  ]
}
```

> 시간별 API 메트릭 스냅샷을 반환합니다. 7일간 보관됩니다.

#### GET /admin/monitoring/cloudwatch
CloudWatch 메트릭 조회

**Auth:** System Admin

**Response:** `MonitoringResponse.CloudWatchMetrics`
```json
{
  "ec2": {
    "cpu_utilization": 15.3,
    "network_in": 1048576.0,
    "network_out": 524288.0
  },
  "rds": {
    "cpu_utilization": 8.5,
    "database_connections": 12,
    "freeable_memory_mb": 512.0,
    "read_iops": 100.0,
    "write_iops": 50.0
  }
}
```

> AWS CloudWatch에서 EC2 인스턴스와 RDS 데이터베이스의 실시간 메트릭을 조회합니다.

#### GET /admin/monitoring/alert-config
모니터링 알림 설정 조회

**Auth:** System Admin

**Response:** `MonitoringResponse.AlertConfig`
```json
{
  "slack_webhook_url": "https://hooks.slack.com/services/T.../B.../xxx",
  "enabled": true,
  "thresholds": {
    "cpu_percent": 80.0,
    "memory_percent": 85.0,
    "hikari_active_percent": 90.0,
    "error_rate_percent": 5.0
  }
}
```

#### PUT /admin/monitoring/alert-config
모니터링 알림 설정 수정

**Auth:** System Admin

**Request:**
| Field | Type | Required | Description |
|-------|------|:--------:|-------------|
| slack_webhook_url | string | X | Slack Webhook URL |
| enabled | boolean | X | 알림 활성화 여부 |

**Response:** `MonitoringResponse.AlertConfig`

> 임계치 초과 시 Slack 채널로 자동 알림이 발송됩니다. 동일 알림은 10분 쿨다운 적용.

#### POST /admin/monitoring/alert-test
테스트 알림 발송

**Auth:** System Admin

**Response:**
```json
{ "message": "테스트 알림이 발송되었습니다" }
```

> 설정된 Slack Webhook으로 테스트 메시지를 발송하여 연동 상태를 확인합니다.

---

### 34. WebSocket (Real-time Sync)

> STOMP over WebSocket (SockJS fallback)
> Premium+ 보드 전용 실시간 동기화

#### 연결 정보

| 항목 | 값 |
|------|-----|
| Endpoint | `/ws` (SockJS) |
| Protocol | STOMP 1.2 |
| 인증 | STOMP CONNECT 프레임의 `Authorization: Bearer {JWT}` 헤더 |
| Fallback | SockJS (XHR-streaming, XHR-polling 등) |

#### CONNECT
WebSocket 연결

**Auth:** User (JWT 필수)

STOMP CONNECT 프레임에 JWT 토큰을 포함하여 인증합니다.

```
CONNECT
Authorization:Bearer eyJ...
accept-version:1.2
host:localhost

\0
```

> `Authorization` 헤더 또는 `token` 헤더로 JWT를 전달할 수 있습니다.
> 유효하지 않거나 누락된 JWT의 경우 연결이 거부됩니다.

#### SUBSCRIBE /topic/board/{boardId}
보드 이벤트 구독

**Auth:** User (Premium+ 보드 구독 필수)

> Standard 티어 보드에 대한 구독은 차단됩니다 (5분 TTL 캐시 적용).

**Event Payload:** `WebSocketEvent`
```json
{
  "type": "FEATURE_CREATED",
  "board_id": "uuid",
  "user_id": "uuid",
  "user_name": "홍길동",
  "timestamp": "2026-02-13T10:00:00",
  "data": { ... }
}
```

> `data` 필드에는 각 이벤트 유형에 해당하는 기존 Response DTO가 포함됩니다.
> 클라이언트는 `user_id`를 비교하여 자신이 발생시킨 이벤트를 필터링할 수 있습니다.

#### SUBSCRIBE /topic/board/{boardId}/user/{userId}
개인 알림 구독

**Auth:** User (Premium+ 보드 구독 필수)

> 특정 사용자에게만 전달되는 이벤트를 수신합니다 (알림 등).

#### BoardEventType (27개 이벤트 타입)

| Category | Event Type | Description |
|----------|-----------|-------------|
| Feature | `FEATURE_CREATED` | Feature 생성 |
| Feature | `FEATURE_UPDATED` | Feature 수정 |
| Feature | `FEATURE_DELETED` | Feature 삭제 |
| Feature | `FEATURES_REORDERED` | Feature 순서 변경 |
| Task | `TASK_CREATED` | Task 생성 |
| Task | `TASK_UPDATED` | Task 수정 |
| Task | `TASK_DELETED` | Task 삭제 |
| Task | `TASK_MOVED` | Task 블록 이동 |
| Block | `BLOCK_CREATED` | 블록 생성 |
| Block | `BLOCK_UPDATED` | 블록 수정 |
| Block | `BLOCK_DELETED` | 블록 삭제 |
| Block | `BLOCKS_REORDERED` | 블록 순서 변경 |
| Comment | `COMMENT_CREATED` | 댓글 생성 |
| Comment | `COMMENT_UPDATED` | 댓글 수정 |
| Comment | `COMMENT_DELETED` | 댓글 삭제 |
| Comment | `COMMENT_REACTION_TOGGLED` | 댓글 리액션 토글 |
| Checklist | `CHECKLIST_CREATED` | 체크리스트 생성 |
| Checklist | `CHECKLIST_UPDATED` | 체크리스트 수정 |
| Checklist | `CHECKLIST_DELETED` | 체크리스트 삭제 |
| Checklist | `CHECKLIST_TOGGLED` | 체크리스트 완료 토글 |
| Board | `BOARD_UPDATED` | 보드 수정 |
| Member | `MEMBER_JOINED` | 멤버 참가 |
| Member | `MEMBER_LEFT` | 멤버 퇴장 |
| Member | `MEMBER_UPDATED` | 멤버 정보 수정 |
| Notification | `NOTIFICATION_CREATED` | 알림 생성 |
| Presence | `PRESENCE_JOINED` | 실시간 접속 |
| Presence | `PRESENCE_LEFT` | 실시간 퇴장 |

#### Message Broker 구성

| 항목 | 값 |
|------|-----|
| Broker Prefix | `/topic`, `/queue` |
| Application Prefix | `/app` |
| Broker Type | SimpleBroker (local/dev), Redis Pub/Sub (prod 확장 가능) |

---

## 에러 코드

| 코드 | 설명 |
|------|------|
| C001 | 공통 - 잘못된 입력값 |
| C002 | 공통 - 내부 서버 오류 |
| R001 | Rate Limiting - 요청 과다 |
| A001 | 인증 - 이미 사용 중인 이메일 |
| A002 | 인증 - 잘못된 자격 증명 |
| A003 | 인증 - 유효하지 않은 토큰 |
| A004 | 인증 - 만료된 토큰 |
| A005 | 인증 - 인증 필요 |
| A006 | 인증 - 유효하지 않은 Google 토큰 |
| A007 | 인증 - OAuth 이메일 미인증 |
| A008 | 인증 - 비밀번호 보안 요구사항 미충족 |
| A009 | 인증 - 이메일 인증 필요 |
| A010 | 인증 - 인증 링크 만료 |
| A011 | 인증 - 유효하지 않은 인증 링크 |
| A012 | 인증 - 이미 사용된 인증 링크 |
| A013 | 인증 - 인증 이메일 재전송 제한 |
| A014 | 인증 - 이미 인증된 이메일 |
| A015 | 인증 - 비밀번호 재설정 링크 만료 |
| A016 | 인증 - 유효하지 않은 비밀번호 재설정 링크 |
| A017 | 인증 - 이미 사용된 비밀번호 재설정 링크 |
| A018 | 인증 - 비밀번호 재설정 이메일 재전송 제한 |
| U001 | 사용자 - 사용자 없음 |
| U002 | 사용자 - 현재 비밀번호 불일치 |
| U003 | 사용자 - 보드 Owner 탈퇴 불가 |
| B001 | 보드 - 보드 없음 |
| B002 | 보드 - 접근 권한 없음 |
| B003 | 보드 - 보드 정지 상태 |
| B004 | 보드 - Premium 전용 기능 |
| BL001 | 블록 - 블록 없음 |
| BL002 | 블록 - 고정 블록 삭제 불가 |
| BL003 | 블록 - 고정 블록 수정 불가 |
| F001 | Feature - Feature 없음 |
| T001 | Task - Task 없음 |
| T002 | Task - 이동 불가 블록 |
| T003 | Task - Standard 보드 제한 초과 |
| TG001 | 태그 - 태그 없음 |
| TG002 | 태그 - 중복 이름 |
| CL001 | 체크리스트 - 항목 없음 |
| SC001 | 스케줄 - 스케줄 블록 없음 |
| M001 | 멤버 - 멤버 없음 |
| M002 | 멤버 - 이미 멤버 |
| M003 | 멤버 - Owner 제거 불가 |
| M004 | 멤버 - Owner 역할 변경 불가 |
| I001 | 초대 - 초대 링크 없음 |
| I002 | 초대 - 만료된 링크 |
| I003 | 초대 - 유효하지 않은 링크 |
| S001 | 구독 - 구독 없음 |
| S002 | 구독 - 체험 기간 만료 |
| S003 | 구독 - 결제 필요 |
| S004 | 구독 - 멤버 수 제한 초과 |
| MS001 | 마일스톤 - 마일스톤 없음 |
| MS002 | 마일스톤 - 이미 연결된 Feature |
| MS003 | 마일스톤 - 할당 정보 없음 |
| MS004 | 마일스톤 - 이미 할당된 멤버 |
| W001 | 가중치 - 가중치 레벨 없음 |
| CM001 | 댓글 - 댓글 없음 |
| CM002 | 댓글 - 본인 댓글만 수정/삭제 가능 |
| DC001 | 데일리 체크리스트 - 항목 없음 |
| DC002 | 데일리 체크리스트 - 해당 날짜에 이미 추가됨 |
| N001 | 알림 - 알림 없음 |
| FL001 | 파일 - 파일 크기 5MB 초과 |
| FL002 | 파일 - 허용되지 않는 파일 형식 (jpg/png/gif/webp만 가능) |
| FL003 | 파일 - 첨부파일 없음 |
| FL004 | 파일 - 첨부파일 최대 5개 초과 |
| FL005 | 파일 - 임시 파일 없음 |
| AD001 | 관리자 - 관리자 권한 필요 |
| AD002 | 관리자 - 마지막 Admin 제거 불가 |
| AD003 | 관리자 - 본인 역할 강등 불가 |
| SL001 | Slack - 유효하지 않은 Webhook URL |
| SL002 | Slack - Webhook 전송 실패 |
| SK004 | Slack - Slack 기능 Premium 전용 |
| IQ001 | 문의 - 문의 없음 |
| IQ002 | 문의 - 본인 문의만 조회 가능 |
| SYS001 | 시스템 - 유지보수 모드 |
| AR001 | AI 리포트 - 리포트 없음 |
| AR002 | AI 리포트 - 리포트 생성 실패 |
| AR003 | AI 리포트 - AI 서비스 불가 |
| AR004 | AI 리포트 - 일일 한도 초과 |
| NT001 | 노트 - 노트 없음 |
| NT002 | 노트 - 폴더가 아닌 항목에 하위 노트 불가 |
| MT001 | 미팅 - 미팅 없음 |
| MT002 | 미팅 - 권한 없음 |
| MN001 | 모니터링 - 설정 없음 |
